function f1() {
    for(let x=1;x<16;x++){
        document.getElementById("seb").style.top = Math.random()*300 + "px";
        document.getElementById("seb").style.left = Math.random()*300 + "px";
    }
}

function showAlert(){
    const sAlert="thenkuuu!!";
    alert(sAlert);
}